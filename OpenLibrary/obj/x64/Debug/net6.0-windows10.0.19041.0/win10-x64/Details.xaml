<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="OpenLibrary.Views.Details"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OpenLibrary"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Frame x:ConnectionId='2' x:Name="bookFrame" />
        <!-- Visszafelé nyíl gomb -->
        <Button x:ConnectionId='3' Grid.Row="0" Margin="10" VerticalAlignment="Top" HorizontalAlignment="Left"                              >
            <SymbolIcon Symbol="Back"/>
        </Button>

        <!-- Tartalom -->
        <StackPanel Grid.Row="1" Margin="10">
            <TextBlock Text="{Binding Key}" FontSize="20" />
            <!-- Egy Popup elem a kép megjelenítéséhez -->
            <Popup x:ConnectionId='4' x:Name="imagePopup">
                <Grid>
                    <Border Background="White" Opacity="0.8"/>
                    <!-- Háttér maszkolása -->
                    <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
                        <Image x:ConnectionId='8' x:Name="image" Width="400" Height="600" Source="{Binding CoverImageUrl}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Grid>
                    <!-- X ikon -->
                    <Button x:ConnectionId='7'                           HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,10,10,0">
                        <SymbolIcon Symbol="Cancel"/>
                    </Button>
                </Grid>
            </Popup>

            <!-- Egy gomb a popup megjelenítéséhez -->
            <Button x:ConnectionId='5' Grid.Row="1" Margin="10"                           >
                <Image Width="100" Height="150" Source="{Binding CoverImageUrl}"/>
            </Button>
            <TextBlock Text="{Binding Title}" FontSize="20"/>
            <StackPanel x:ConnectionId='6' x:Name="authorPanel" Margin="0,5,0,0"/>
            <TextBlock FontSize="13" Text="{Binding FirstPublishDate}"/>
            <TextBlock FontSize="13" Text="{Binding SubjectsAsString}" TextWrapping="Wrap"/>
            <TextBlock FontSize="13" Text="{Binding Description}" TextWrapping="Wrap"/>
        </StackPanel>
    </Grid>
</Page>

